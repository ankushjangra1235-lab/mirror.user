# Secret key for Flask sessions
SECRET_KEY=mirror-user-secret-key-121

# Connects to PostgreSQL in production (e.g. from Railway)
# For local testing, if empty or default, it falls back to a local sqlite mirror.db
DATABASE_URL=postgresql://postgres:password@localhost:5432/mirrordb
